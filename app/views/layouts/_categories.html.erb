<br>
<% @categories.each do |category| %>
<div class="cat">
<p><a href="/category/<%= category.title %>" class="button"><%= category.title %></a></p>
<%= link_to (image_tag category.image, :class=>"catIm"), category %>
</div>
<% end %>